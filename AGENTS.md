# Agent Operating Notes

## Important

- For browser debugging tasks, do not rely on headless-only flow or periodic screenshots.
- Always run with a real visible browser window and attach/control it in a way the user can watch in real time (for example, via Chrome CDP attach).
- Keep the user and agent on the same live page context while debugging so the user can issue precise step-by-step instructions.
